# AGENTS.md

## Agent skills

### Issue tracker

Issues sống thành file markdown trong repo, dưới `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Năm nhãn triage mặc định, tên nhãn trùng tên vai trò. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` ở gốc repo. See `docs/agents/domain.md`.
